#include <cstdio>
#include <cstring>
#include <algorithm>

using namespace std;

const int MAXN = 1e3;
int memo[MAXN+5];
int arr[MAXN+5];

int n;

int DP(){
	
}

int main(){
	memset(memo,-1,sizeof(memo));
	scanf("%d",&n);
	for(int i = 0; i < n ; i++)
		scanf();
	printf();
}